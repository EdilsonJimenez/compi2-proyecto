CREATE OR REPLACE DATABASE MiBase1 OWNER = Oscar MODE = 1;

CREATE TABLE Estudiante   (
    carne INT NOT NULL,
    nombre varchar(20+6),
    apellido TEXT NOT NULL,
);
SELECT EXTRACT ( YEAR FROM TIMESTAMP '25-6-12 algo');
SELECT DATE_PART ( 'seconds' , INTERVAL '4 hours 23 minutes 56 seconds' );
SELECT CURRENT_DATE;
SELECT CURRENT_TIME;

SELECT TIMESTAMP 'Hora';
SELECT now();

CREATE TYPE MOOD AS ENUM ('abc', 'cda');
CREATE TYPE MOOD AS ENUM ('integer');
