/*CREATE TABLE Estudiante (
    carne INT NOT NULL
    nombre varchar(20),
    apellido TEXT NOT NULL,
);
*/
/*CREATE TABLE Estudiante (
    carne INT NOT NULL PRIMARY KEY
    nombre TEXT NOT NULL,
    apellido TEXT NOT NULL,
    CONSTRAINT carne CHECK ( carne > 2000 )
    CONSTRAINT campo UNIQUE,
    CONSTRAINT estudio CHECK( a > b),
    UNIQUE( abc, cba),
    CONSTRAINT miLLave PRIMARY KEY ( carne, codigo),
    PRIMARY KEY (carne),
    FOREIGN KEY (llave, carne, otro) REFERENCES mi_clase(otra, lista)
); */

/*
CREATE OR REPLACE DATABASE IF NOT EXISTS miBase OWNER = Oscar MODE = 3;
SHOW DATABASES;
DROP TABLE Mitabla;
ALTER DATABASE miBD Owner TO SESSION_USER;
ALTER DATABASE miBD Rename TO Administrador;
DROP DATABASE miBasedeDatos;
DROP DATABASE IF EXISTS miBasedeDatos;
DELETE FROM products WHERE price = 10;

INSERT INTO products,producto2 VALUES (9+9+9*6);
*/

SELECT EXTRACT ( YEAR FROM TIMESTAMP '25-6-12 algo');
SELECT DATE_PART ( 'seconds' , INTERVAL '4 hours 23 minutes 56 seconds' );
SELECT CURRENT_DATE;
SELECT CURRENT_TIME;

SELECT TIMESTAMP 'Hora';
SELECT now();

CREATE TYPE MOOD AS ENUM ('abc', 'cda', 'miTipo');
CREATE TYPE MOOD AS ENUM ('integer');