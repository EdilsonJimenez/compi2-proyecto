CREATE OR REPLACE DATABASE IF NOT EXISTS miBase OWNER = Oscar MODE = 3;

/*CREATE TABLE Estudiante (
    carne INT NOT NULL PRIMARY KEY
    nombre TEXT NOT NULL,
    apellido TEXT NOT NULL,
    CONSTRAINT carne CHECK ( carne > 2000 )
    CONSTRAINT campo UNIQUE,
    CONSTRAINT estudio CHECK( a > b),
    UNIQUE( abc, cba),
    CONSTRAINT miLLave PRIMARY KEY ( carne, codigo),
    PRIMARY KEY (carne),
    FOREIGN KEY (llave, carne, otro) REFERENCES mi_clase(otra, lista)
); */


DROP TABLE Mitabla;

/*
DELETE FROM products WHERE price = 10;

INSERT INTO products,producto2 VALUES (9+9+9*6);*/


select gee.ss as d, sd as s from tabla1 as t1 group by id;

select * from tabla1 as t where 2*3;

select gee.ss as d, sd as s from tabla1 as t1 where 4*3;


select tabla1.ss as d, sd as s from tabla1 as t1 where 4=3;

ALTER TABLE tabla1 DROP COLUMN columna1, columna2;

ALTER TABLE table1 RENAME COLUMN columna1 TO columna11;

ALTER TABLE tabla1 DROP CONSTRAINT pk_id;

ALTER TABLE tabla2 ALTER COLUMN id SET NOT NULL;

ALTER TABLE tabla2 ADD FOREIGN KEY ( columa1 ) REFERENCES columna2;

ALTER TABLE table1 ADD CONSTRAINT some_name UNIQUE (column1);
