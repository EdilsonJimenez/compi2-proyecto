CREATE DATABASE MiBase1 OWNER = Oscar MODE = 1;

USE MiBase1;

CREATE TABLE profesional(
    carne INT,
    nombre INT,
    apellido VARCHAR(20),
    bandera BOOLEAN
);

INSERT INTO profesional VALUES(300,123,'Mazariegos', TRUE);
INSERT INTO profesional VALUES(400,456, 'Lopez', TRUE);
INSERT INTO profesional VALUES(250,789,'Mazariegos', FALSE);
INSERT INTO profesional VALUES(50,899,'Mazariegos', FALSE);
DELETE FROM profesional WHERE carne > 50 AND nombre = 789;
UPDATE profesional SET carne = 999 , WHERE carne = 50 AND nombre = 789;

or str(temporal[index].tipo) == 'INTEGER' or str(temporal[index].tipo) == 'INT' or str(temporal[index].tipo) == 'BIGINT' or str(temporal[index].tipo) == 'DECIMAL' or str(temporal[index].tipo) == 'REAL' or str(temporal[index].tipo) == 'FLOAT' or str(temporal[index].tipo) == 'MONEY'
or str(cc.val) == "FALSE"